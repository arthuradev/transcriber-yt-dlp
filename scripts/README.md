# Scripts Placeholder

Expected scripts:

- `quality_gate.py`
- `bootstrap.ps1`
- `build_exe.ps1`
- `build_installer.ps1`

Only `quality_gate.py` is expected in Phase 1. Bootstrap and packaging scripts come later.
