"""Assets package placeholder.

Runtime resources (ASCII art, UI resources) currently live under the repo-root
``assets/`` directory. This package exists to reserve the architectural slot for
packaged resources in a later phase.
"""
