# Phase 10 — Playlists, batch URLs, folders, duplicates

## Tag
`v0.10.0`

## Branch
`phase/10-playlists-batch-urls-folders-duplicates`

## Goal
Add playlist support, `.txt` batch input, folder organization, duplicate/history/archive basics.

## Explicit non-scope
Respect batch limits.

## Required work
- Follow architecture rules.
- Add or update tests.
- Update documentation if behavior/config/architecture changes.
- Update `CHANGELOG.md`.
- Update `TASKS.md`.
- Run quality gates.

## Acceptance criteria
- Phase goal is implemented.
- No out-of-scope work is included.
- Quality gates pass.
- No secrets/private files are committed.
- Phase branch is merged into `main`.
- Annotated tag `v0.10.0` is created.
- GitHub Release for `v0.10.0` is created.
- Claude asks the user whether to proceed to Phase 11.

## Release note template
```md
## v0.10.0 — Phase 10: Playlists, batch URLs, folders, duplicates

### Added
- ...

### Changed
- ...

### Fixed
- ...

### Tests
- ...

### Known limitations
- ...
```
